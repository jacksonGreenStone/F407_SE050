/**
 * @file tls_client.h
 * @brief TLS client header
 */

#ifndef TLS_CLIENT_H
#define TLS_CLIENT_H

#include <stdint.h>

/**
 * @brief Run TLS client example
 * @retval 0 if successful, non-zero otherwise
 */
int tls_client_run(void);

#endif /* TLS_CLIENT_H */